<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= typeof title !== 'undefined' ? title : 'Blogify' %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css"/>
</head>
<body>
  <!-- Header Navbar -->
  <nav class="navbar navbar-expand-lg navbar-light bg-light mb-4 p-3">
    <div class="container">
  <a class="navbar-brand fw-bold fs-4 me-2" href="/blog">Blogify</a>
      <div class="d-flex align-items-center gap-2 ms-auto">
        <% if (typeof user !== 'undefined' && user && user.username) { %>
          <span class="me-3 fw-semibold text-primary">Welcome, <%= user.username %>!</span>
          <a href="/users/manage" class="btn btn-outline-secondary btn-sm me-2"><i class="fa fa-users me-1"></i>Manage Users</a>
          <a href="/users/profile" class="btn btn-outline-primary btn-sm me-2"><i class="fa fa-user me-1"></i>Profile</a>
          <a href="/logout" class="btn btn-danger btn-sm me-2">Logout</a>
        <% } else { %>
          <a href="/login" class="btn btn-outline-dark btn-sm me-2">Login</a>
          <a href="/signup" class="btn btn-dark btn-sm me-2">Sign Up</a>
        <% } %>
        <a href="/blog" class="btn btn-outline-success btn-sm"><i class="fa fa-home me-1"></i>Home</a>
      </div>
    </div>
  </nav>
